<%= render partial: "layouts/form-container" %>
<%= render partial: "layouts/profile-navbar" %>
<% if current_user && @user == current_user %>
	<a href="<%= new_code_project_url(subdomain: current_user.username) %>" class="tabbed-link no-trans">
		Add Project<i class="icon-plus"></i>
	</a>
<% end %>
<div class="content">
	<%= will_paginate @projects %>
	<%= render @projects %>
</div>